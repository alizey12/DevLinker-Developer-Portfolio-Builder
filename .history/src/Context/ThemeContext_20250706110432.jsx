import {useState,useEffect,useContext,createContext  } from "react";
export const ThemeContext = createContext();

export default ThemeProvider=({Chil})=>{


    return(
        <ThemeContext.Provider>

        </ThemeContext.Provider>
    );
};